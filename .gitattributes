# 기본은 Git 이 알아서 처리
* text=auto

# 셸 스크립트는 반드시 LF.
# 윈도우에서 CRLF 로 커밋되면 리눅스 컨테이너에서
# "bad interpreter: /usr/bin/env bash^M" 오류가 납니다.
*.sh text eol=lf
gradlew text eol=lf
Dockerfile text eol=lf
*.yml text eol=lf
