<?xml version="1.0" encoding="utf-8"?>
<!-- MY 탭의 저장한 코스 한 줄. -->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/space_m"
    android:clickable="true"
    android:focusable="true"
    app:cardBackgroundColor="@color/bg_card"
    app:cardCornerRadius="@dimen/radius_card"
    app:cardElevation="0dp"
    app:strokeColor="@color/card_stroke"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/space_l">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/tv_saved_title"
                style="@style/Text.SectionTitle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:ellipsize="end"
                android:maxLines="1"
                tools:text="내장산국립공원 외 4곳" />

            <ImageButton
                android:id="@+id/btn_saved_delete"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/my_delete_course"
                android:src="@drawable/ic_delete"
                app:tint="@color/text_tertiary" />
        </LinearLayout>

        <TextView
            android:id="@+id/tv_saved_date"
            style="@style/Text.Caption"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:text="8월 4일 저장 · 5곳 · 5시간 21분" />

        <TextView
            android:id="@+id/tv_saved_summary"
            style="@style/Text.Body"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_s"
            android:ellipsize="end"
            android:maxLines="2"
            tools:text="오전 동향인 정읍사예술회관을 시작으로, 한낮 강한 빛을 피해 실내로 이동합니다." />

        <TextView
            android:id="@+id/tv_saved_stops"
            style="@style/Text.Caption"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_s"
            android:textColor="@color/maple_600"
            tools:text="10:16 정읍사예술회관 → 11:22 씨아전복 → 12:31 정읍문화원" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
