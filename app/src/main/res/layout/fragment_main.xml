<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_page">

    <!--
      헤더(제목 + 촬영 환경)는 스크롤하면 접히고,
      카테고리 필터 줄은 상단에 고정되어 언제든 바꿀 수 있게 했다.
    -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/bg_page"
        android:stateListAnimator="@null"
        app:elevation="0dp"
        app:liftOnScroll="false">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="@dimen/space_l"
            app:layout_scrollFlags="scroll|snap">

            <TextView
                android:id="@+id/tv_main_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="@dimen/screen_padding"
                android:layout_marginTop="@dimen/space_l"
                android:text="@string/app_name"
                android:textAppearance="@style/Text.Display" />

            <TextView
                android:id="@+id/tv_tagline"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="@dimen/screen_padding"
                android:layout_marginTop="2dp"
                android:text="@string/app_tagline"
                android:textAppearance="@style/Text.Caption" />

            <com.google.android.material.card.MaterialCardView
                android:id="@+id/weatherCard"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="@dimen/screen_padding"
                android:layout_marginTop="@dimen/space_l"
                app:cardCornerRadius="24dp"
                app:cardElevation="6dp"
                app:strokeWidth="0dp">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:background="@drawable/bg_weather_gradient"
                    android:orientation="vertical"
                    android:paddingHorizontal="@dimen/space_xl"
                    android:paddingVertical="18dp">

                    <LinearLayout
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:gravity="center_vertical"
                        android:orientation="horizontal">

                        <ImageView
                            android:layout_width="15dp"
                            android:layout_height="15dp"
                            android:alpha="0.85"
                            android:contentDescription="@null"
                            android:src="@drawable/ic_sun"
                            app:tint="@color/white" />

                        <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_marginStart="6dp"
                            android:alpha="0.85"
                            android:letterSpacing="0.04"
                            android:text="@string/shooting_condition"
                            android:textColor="@color/white"
                            android:textSize="12sp" />
                    </LinearLayout>

                    <TextView
                        android:id="@+id/tv_weather_status"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="10dp"
                        android:letterSpacing="-0.02"
                        android:text="@string/weather_loading"
                        android:textColor="@color/white"
                        android:textSize="23sp"
                        android:textStyle="bold"
                        tools:text="지금 정읍은 29.2℃" />

                    <LinearLayout
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="14dp"
                        android:background="@drawable/bg_golden_pill"
                        android:gravity="center_vertical"
                        android:orientation="horizontal"
                        android:paddingHorizontal="12dp"
                        android:paddingVertical="7dp">

                        <View
                            android:layout_width="7dp"
                            android:layout_height="7dp"
                            android:background="@drawable/dot_indicator_active"
                            android:backgroundTint="@color/golden_300" />

                        <TextView
                            android:id="@+id/tv_golden_hour"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_marginStart="8dp"
                            android:text="@string/golden_hour_loading"
                            android:textColor="@color/golden_200"
                            android:textSize="13sp"
                            android:textStyle="bold"
                            tools:text="일몰까지 2시간 30분" />
                    </LinearLayout>
                </LinearLayout>
            </com.google.android.material.card.MaterialCardView>
        </LinearLayout>

        <!-- 상단 고정 : 카테고리 필터 -->
        <HorizontalScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/bg_page"
            android:paddingVertical="@dimen/space_s"
            android:scrollbars="none">

            <com.google.android.material.chip.ChipGroup
                android:id="@+id/chip_group_category"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingHorizontal="@dimen/screen_padding"
                app:chipSpacingHorizontal="8dp"
                app:selectionRequired="true"
                app:singleLine="true"
                app:singleSelection="true">

                <com.google.android.material.chip.Chip
                    android:id="@+id/chip_all"
                    style="@style/Chip.Category"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:checked="true"
                    android:text="@string/category_all"
                    app:chipIcon="@drawable/ic_cat_all" />

                <com.google.android.material.chip.Chip
                    android:id="@+id/chip_nature"
                    style="@style/Chip.Category"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/category_nature"
                    app:chipIcon="@drawable/ic_cat_nature" />

                <com.google.android.material.chip.Chip
                    android:id="@+id/chip_culture"
                    style="@style/Chip.Category"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/category_culture"
                    app:chipIcon="@drawable/ic_cat_culture" />

                <com.google.android.material.chip.Chip
                    android:id="@+id/chip_leports"
                    style="@style/Chip.Category"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/category_leports"
                    app:chipIcon="@drawable/ic_cat_leports" />

                <com.google.android.material.chip.Chip
                    android:id="@+id/chip_food"
                    style="@style/Chip.Category"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/category_food"
                    app:chipIcon="@drawable/ic_cat_food" />
            </com.google.android.material.chip.ChipGroup>
        </HorizontalScrollView>
    </com.google.android.material.appbar.AppBarLayout>

    <!-- ================= 촬영지 목록 ================= -->
    <!--
      당겨서 새로고침. 포토스코어는 골든아워·기온·강수에 따라 바뀌므로
      앱을 켜 둔 채 시간이 지나면 다시 계산해 줄 수 있어야 합니다.
    -->
    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipe_refresh"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_photo_spots"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:overScrollMode="never"
            android:paddingTop="@dimen/space_s"
            android:paddingBottom="96dp"
            android:scrollbars="none"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            tools:itemCount="3"
            tools:listitem="@layout/item_spot_card" />
    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

    <!-- ================= 빈 상태 ================= -->
    <LinearLayout
        android:id="@+id/layout_empty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"
        android:paddingVertical="64dp"
        android:visibility="gone"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        tools:visibility="gone">

        <ImageView
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:background="@drawable/bg_empty_circle"
            android:contentDescription="@null"
            android:padding="18dp"
            android:src="@drawable/baseline_image_24"
            app:tint="@color/text_tertiary" />

        <TextView
            android:id="@+id/tv_empty_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_l"
            android:text="@string/empty_title"
            android:textColor="@color/text_secondary"
            android:textSize="15sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tv_empty_desc"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_xs"
            android:text="@string/empty_desc"
            android:textAppearance="@style/Text.Caption" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_retry"
            style="@style/Widget.Material3.Button.TonalButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_l"
            android:text="@string/retry"
            android:visibility="gone"
            app:cornerRadius="16dp" />
    </LinearLayout>

    <!-- ================= 지도 FAB (항상 노출) ================= -->
    <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
        android:id="@+id/btn_open_naver_map"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="@dimen/space_l"
        android:layout_marginBottom="20dp"
        android:text="@string/open_map_short"
        android:textColor="@color/white"
        app:backgroundTint="@color/naver_green"
        app:elevation="8dp"
        app:icon="@drawable/ic_near_me"
        app:iconTint="@color/white"
        app:rippleColor="#33FFFFFF" />

    <com.google.android.material.progressindicator.CircularProgressIndicator
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:indeterminate="true"
        android:visibility="gone"
        app:indicatorColor="@color/brand_500"
        app:trackCornerRadius="4dp" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
