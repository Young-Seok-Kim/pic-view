<?xml version="1.0" encoding="utf-8"?>
<!-- 포토스코어 항목 하나. 이름 · 점수 · 막대 · 근거 한 줄. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/space_m"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tv_factor_label"
            style="@style/Text.Body"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="@color/text_primary"
            android:textStyle="bold"
            tools:text="빛 조건" />

        <TextView
            android:id="@+id/tv_factor_value"
            style="@style/Text.Body"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textStyle="bold"
            tools:text="20 / 20" />
    </LinearLayout>

    <ProgressBar
        android:id="@+id/pb_factor"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="6dp"
        android:layout_marginTop="@dimen/space_xs"
        android:max="100"
        android:progressDrawable="@drawable/bg_factor_bar"
        tools:progress="100" />

    <TextView
        android:id="@+id/tv_factor_reason"
        style="@style/Text.Caption"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_xs"
        tools:text="지금 골든아워이고 서향이에요. 최적 조건이에요" />
</LinearLayout>
