<?xml version="1.0" encoding="utf-8"?>
<!-- 프레임으로 만든 사진 한 칸. 종류(프레임/네컷)와 만든 날을 아래 띠에 답니다. -->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="220dp"
    android:layout_margin="4dp"
    android:clickable="true"
    android:focusable="true"
    app:cardBackgroundColor="@color/bg_card_muted"
    app:cardCornerRadius="12dp"
    app:cardElevation="0dp"
    app:strokeColor="@color/card_stroke"
    app:strokeWidth="1dp">

    <ImageView
        android:id="@+id/iv_framed"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@string/cd_frame_preview"
        android:scaleType="fitCenter"
        tools:src="@drawable/bg_image_placeholder" />

    <TextView
        android:id="@+id/tv_framed_caption"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="@drawable/bg_scrim_bottom"
        android:ellipsize="end"
        android:maxLines="1"
        android:paddingHorizontal="8dp"
        android:paddingTop="@dimen/space_m"
        android:paddingBottom="6dp"
        android:textColor="@color/white"
        android:textSize="11sp"
        android:textStyle="bold"
        tools:text="사진 프레임 · 9월 9일" />
</com.google.android.material.card.MaterialCardView>
