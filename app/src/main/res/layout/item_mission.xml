<?xml version="1.0" encoding="utf-8"?>
<!--
    미션 하나.

    왼쪽 원이 유형(빛·날씨·계절…)을 색으로 말하고, 오른쪽이 무엇을 하면
    되는지를 말합니다. 아이콘만으로는 ⇄ 가 비교인지 이동인지 갈리므로
    언제나 글자와 함께 둡니다.
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/card_mission"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/space_m"
    app:cardBackgroundColor="@color/bg_card"
    app:cardCornerRadius="@dimen/radius_card"
    app:cardElevation="0dp"
    app:strokeColor="@color/card_stroke"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="@dimen/space_l">

        <FrameLayout
            android:id="@+id/layout_mission_badge"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="@drawable/bg_mission_circle">

            <TextView
                android:id="@+id/tv_mission_emoji"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:textSize="19sp"
                tools:text="☀" />

            <!-- 완료한 미션에만 체크를 더합니다. 아이콘은 그대로 둡니다. -->
            <TextView
                android:id="@+id/tv_mission_check"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom|end"
                android:background="@drawable/bg_mission_check"
                android:paddingHorizontal="3dp"
                android:text="@string/mission_check"
                android:textColor="@color/white"
                android:textSize="10sp"
                android:visibility="gone"
                tools:visibility="visible" />
        </FrameLayout>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/space_m"
            android:layout_weight="1"
            android:orientation="vertical">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <TextView
                    android:id="@+id/tv_mission_name"
                    style="@style/Text.SectionTitle"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:ellipsize="end"
                    android:maxLines="1"
                    tools:text="오늘의 빛 사냥꾼" />

                <TextView
                    android:id="@+id/tv_mission_count"
                    style="@style/Text.Caption"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textStyle="bold"
                    tools:text="1 / 2" />
            </LinearLayout>

            <TextView
                android:id="@+id/tv_mission_req"
                style="@style/Text.Body"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                tools:text="황금시간에 포토스코어 70점 이상인 곳에서 사진 2장 남기기" />

            <ProgressBar
                android:id="@+id/pb_mission"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="match_parent"
                android:layout_height="6dp"
                android:layout_marginTop="@dimen/space_s"
                android:max="100"
                android:progressDrawable="@drawable/bg_factor_bar"
                tools:progress="50" />

            <!--
              완료 문구 또는 다음 행동.
              조건을 그대로 되풀이하면 위 줄을 두 번 읽게 됩니다.
            -->
            <TextView
                android:id="@+id/tv_mission_status"
                style="@style/Text.Caption"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/space_s"
                tools:text="다음 황금시간에 한 장 더 남겨보세요" />
        </LinearLayout>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
