# 빌드용 기본값 (git 추적됨 — 실제 키를 여기 넣지 마세요)
#
# 실제 키는 local.properties 에 넣습니다.
# 이 파일은 키가 없는 환경(다른 PC·CI)에서도 빌드가 깨지지 않게 하는 용도입니다.
#
# 주의: secrets 플러그인은 '빈 값'을 무시하고 BuildConfig 에 넣지 않습니다.
#       그래서 빈 문자열 대신 아래 센티널을 씁니다.
#       코드에서는 ApiKeys.isUsable() 로 판별합니다.
TOUR_API_KEY=UNSET
NAVER_CLIENT_ID=UNSET
GEMINI_API_KEY=UNSET
